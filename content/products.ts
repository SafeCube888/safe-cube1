import type { Product, ProductCategory } from '@/types/content';

/**
 * SAFE CUBE Store Products & Categories — Structured Data
 *
 * Content will be populated in Phase 5 from the master brief.
 * No fake products will be created.
 */
export const products: Product[] = [];
export const productCategories: ProductCategory[] = [];
